{
  "name": "slideclip",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate",
    "test": "jest --config jest.e2e.config.js --runInBand",
    "tests": "jest --config jest.e2e.config.js --runInBand ./tests/e2e",
    "testServer": "nuxt build && nuxt start --port 3000"
  },
  "dependencies": {
    "@nuxtjs/axios": "^5.12.0",
    "express": "^4.17.1",
    "node-sass": "^5.0.0",
    "nuxt": "^2.14.0",
    "puppeteer": "^5.5.0",
    "sass-loader": "^10.1.1"
  },
  "devDependencies": {
    "jest": "^26.6.3",
    "jest-puppeteer": "^4.4.0"
  }
}
